// Production API base URL — update after deploying the web app
// Leave empty string in development to use the same machine (via Expo dev proxy)
export const API_BASE_URL = process.env.EXPO_PUBLIC_API_URL ?? '';
